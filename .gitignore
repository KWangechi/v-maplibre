# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Nuxt
.nuxt
.output
.data
.nitro
.cache

# Test coverage
coverage

.env

# Temporary files
tmp/
.sisyphus

# Local notes
TODO.local.md

# Generated data (regenerate with: bun run apps/mapcn-vue/scripts/prepare-promap-data.ts)
# promap-data.json is VENDORED — committed to the repo. SimpleMaps
# (one of the three upstream sources) sits behind Cloudflare Bot Fight Mode,
# so CI cannot regenerate it. Maintainer-only regeneration:
#   node apps/mapcn-vue/scripts/prepare-promap-data.ts
# (run locally; bypass SimpleMaps 403 by downloading the ZIP via browser first)
.design-explorations
